import type { NextPage } from "next";
import { AppHead, Nav } from "../components";

const Home: NextPage = () => {
  return (
    <>
      <AppHead />
      <Nav />
    </>
  );
};

export default Home;
